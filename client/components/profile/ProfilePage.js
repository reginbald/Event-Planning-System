import React, { Component, PropTypes } from 'react';

export default class ProfilePage extends Component {
  constructor(props) {
    super(props);
  }

  render() {
    return (
      <h1>Profile</h1>
    );
  }
}
